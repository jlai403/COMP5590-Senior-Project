@using WorkflowManagementSystem.Views
@model List<WorkflowManagementSystem.Models.Workflow.WorkflowDataViewModel>

<div class="workflow-details small-text">
    @foreach (var workflowStep in Model)
    {
        <div class="row detail @Html.GetWorkflowStatusDisplay(workflowStep.State)">
            <div class="row">
                <div class="col-xs-5 right-align bold-text">
                    Role:
                </div>
                <div class="col-xs-7 left-align">
                    @workflowStep.ResponsibleParty
                </div>
            </div>

            <div class="row">
                <div class="col-xs-5 right-align bold-text">
                    Status:
                </div>
                <div class="col-xs-7 left-align">
                    @Html.GetWorkflowStatusDisplay(workflowStep.State)
                </div>
            </div>

            <div class="row">
                <div class="col-xs-5 right-align bold-text">

                    @if (workflowStep.IsApproved())
                    {
                        @:Approved By:
                    }
                    @if (workflowStep.IsRejected())
                    {
                        @:Rejected By:
                    }
                </div>
                <div class="col-xs-7 left-align">
                    @workflowStep.User
                </div>
            </div>
        </div>
    }
</div>